# 네이버 개발자센터 검색 API 키 (https://developers.naver.com/apps)
# 아래 두 값을 채우고 파일명을 .env 로 바꾸면 실시간 데이터로 전환됩니다.
# 키가 없으면 server/seed/*.json 의 시드 데이터로 자동 폴백합니다.
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# (선택) 서버 포트. 기본 3000
# PORT=3000
